using Sistem;

class Program
{
    static void Main()
    {
         Console.Write();
         double a = Convert.ToDouble(Console.Reablene());
         Console.Write();
         double b = Convert.ToDouble(Console.Reablene());
         double c = Math.Sqrt(a * a + b * b);
         Console.WriteLine();
    }
}


using Sistem;

class Program
{
    static void Main()
    {
         Console.Write();
         int number = Conver.ToInt32(Console.ReadLine());
        if (number % 2 == 0)
        {
            Console.Write(чет);
         }
        else 
        {
           Console.Write(нечет); 
        }
 }
} 





using Sistem;

class Program
{
    static void Main()
    {
         Console.Write();
         double a = Convert.ToDouble(Console.Reablene());
         Console.Write();
         double b = Convert.ToDouble(Console.Reablene());
         Console.Write();
         double op = Convert.ToDouble(Console.Reablene());
         douple result = 0;
         switch (op)
         {
              cause "+":
                 result = a + b;
                 break;
             cause "-":
                 result = a - b;
                 break;
             cause "*":
                 result = a * b;
                 break;
             cause "/":
                 if (b != 0)
                    result = a / b;
                 else
                 {
                     Console.WriteLine("Ошибка");
                     return:
                 }
                 break;
    }
     Console.Writleline(result);
}
























         
