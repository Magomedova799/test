using Sistem;

class Program
{
    static void Main()
    {
         Console.Write();
         double a = Convert.ToDouble(Console.Reablene());
         Console.Write();
         double b = Convert.ToDouble(Console.Reablene());
         double c = Math.Sqrt(a * a + b * b);
         Console.WriteLine();
    }
}
